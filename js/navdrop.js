$(window).resize(function () {
            var width = $(window).width();
            if (width > 1200) {
                var width = $(window).width(); if (width > 1200) {
                    document.getElementById("navbarDropdownMenuLink").onclick = "location.href = 'catalog.html'"

                }
            }
        });